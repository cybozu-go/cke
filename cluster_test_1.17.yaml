name: test
options:
  kube-scheduler:
    extenders:
      - "urlPrefix: http://127.0.0.1:8000"
    predicates:
      - "name: some_predicate"
    priorities:
      - "name: some_priority"
  kubelet:
    container_log_max_size: 10Mi
